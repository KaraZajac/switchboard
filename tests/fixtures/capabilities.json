{
  "$comment": "The capabilities both clients ask for, and the rules a name must satisfy. Read by tests/main/capnames.test.ts and the Android SharedCorpusTest, so the desktop and the phone cannot drift apart. Only advertised capabilities are actually requested, which is why a wrong name here fails silently rather than loudly.",
  "requested": [
    "cap-notify",
    "message-tags",
    "batch",
    "labeled-response",
    "echo-message",
    "server-time",
    "sasl",
    "multi-prefix",
    "userhost-in-names",
    "extended-join",
    "account-notify",
    "account-tag",
    "away-notify",
    "chghost",
    "setname",
    "invite-notify",
    "standard-replies",
    "no-implicit-names",
    "monitor",
    "extended-monitor",
    "draft/message-redaction",
    "draft/message-edit",
    "draft/chathistory",
    "draft/read-marker",
    "draft/webpush",
    "draft/multiline",
    "draft/channel-rename",
    "draft/account-registration",
    "draft/metadata-3",
    "draft/metadata-2",
    "draft/event-playback",
    "draft/pre-away",
    "draft/search",
    "draft/auto-join",
    "draft/client-batch",
    "draft/oper-tag",
    "draft/relaymsg",
    "znc.in/self-message",
    "draft/extended-isupport",
    "draft/channel-context",
    "draft/react",
    "draft/unreact",
    "typing",
    "reply"
  ],
  "rules": {
    "lowercase": "Capability names are case-sensitive and every registered one is lowercase. UTF8ONLY is the ISUPPORT token, not a capability.",
    "noClientTagPrefix": "A '+' prefixes a message tag. A CAP REQ token starting with one names a capability that cannot exist.",
    "maxLineBytes": 512,
    "isupportNotCapability": "Bot mode, account extbans and WHOX are announced with the ISUPPORT tokens BOT, ACCOUNTEXTBAN and WHOX. None of them is a capability, so asking for one is asking for something that cannot be granted — and because only advertised names are ever requested, it fails by doing nothing rather than by saying so."
  },
  "notCapabilities": [
    "message-ids",
    "draft/register-before-connect",
    "UTF8ONLY",
    "+typing",
    "+draft/channel-context",
    "draft/no-implicit-names",
    "draft/edit",
    "utf8only",
    "message-redaction",
    "bot",
    "account-extban",
    "whox"
  ],
  "deliberatelyNotRequested": {
    "draft/persistence": "A real capability, and nothing here implements the PERSISTENCE command. Enabling it changes what a server does when a client goes away, which is not something to ask for and then ignore."
  }
}
